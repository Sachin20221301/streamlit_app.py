import setuptools

setuptools.setup(
    name="streamlit_app",
    version="0.1",
    install_requires=[
        "streamlit",
        "ultralyticsplus"
    ],
    packages=setuptools.find_packages(),
)
